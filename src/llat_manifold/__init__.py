"""LLAT_manifold_experiments — idealized perturbation experiments for the LLAT
(DLAMPty / coupled FCNv2) regional model.

The heavy model code (FCNv2, DLAMPty, the coupling transfer) is reused from the
operational repo via the editable-installed ``regional_couple`` package; this
package adds the perturbation-evolution driver, the hook/injection system, and
the advanced diagnostics.
"""

__version__ = "0.1.0"
